{% extends 'base.html.twig' %}

{% block title %}Commentaire Consultant{% endblock %}

{% block body %}
    <h1>Commentaire Consultant</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Description</th>
                <th>Date</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for commentaire_consultant in commentaire_consultants %}
            <tr>
                <td>{{ commentaire_consultant.id }}</td>
                <td>{{ commentaire_consultant.description }}</td>
                <td>{{ commentaire_consultant.date ? commentaire_consultant.date|date('d-m-Y H:i') : '' }}</td>
                <td>
                    <a href="{{ path('commentaire_consultant_show', {'id': commentaire_consultant.id}) }}">Détail</a>
                    <a href="{{ path('commentaire_consultant_edit', {'id': commentaire_consultant.id}) }}">Editer</a>
                    {{ include('commentaire_consultant/_delete_form.html.twig') }}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('commentaire_consultant_new') }}">Create new</a>
{% endblock %}
