{% extends 'base.html.twig' %}

{% block title %}Adresse{% endblock %}

{% block main %}
    <h1>Adresse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adresse.id }}</td>
            </tr>
            <tr>
                <th>Rue</th>
                <td>{{ adresse.rue }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ adresse.ville }}</td>
            </tr>
            <tr>
                <th>Cp</th>
                <td>{{ adresse.cp }}</td>
            </tr>
            <tr>
                <th>Pays</th>
                <td>{{ adresse.pays }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('adresse') }}">Retour à la liset</a>

    <a href="{{ path('adresse_edit', {'id': adresse.id}) }}">editer</a>

    {{ include('adresse/_delete_form.html.twig') }}
{% endblock %}
