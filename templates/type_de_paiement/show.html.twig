{% extends 'base.html.twig' %}

{% block title %}Type de paiement{% endblock %}

{% block main %}
    <h1>Type de paiement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Identifiant</th>
                <td>{{ type_de_paiement.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ type_de_paiement.libelle }}</td>
            </tr>
        </tbody>
    </table>
<div class="consultant_show_btn col-sm-12 group_buttons">
    <button class="btn btn-secondary btn-sm" type="button"> <a href="{{ path('type_de_paiement') }}">Retour à la liste</a></button>
    <button class="btn btn-secondary btn-sm" type="button"> <a href="{{ path('type_de_paiement_edit', {'id': type_de_paiement.id}) }}">Editer</a></button>
    {{ include('type_de_paiement/_delete_form.html.twig') }}
</div>
{% endblock %}
